# MiniMax H3 普通视频续拍 × SelfLift 测试记录

> 测试日期：2026-09-13（Asia/Singapore）
> 目标：修复普通视频续拍在最后 22 帧之后出现的模糊，同时保持人物肢体和运动结构稳定。

## 一、测试链路

```text
源视频 IMAGE/AUDIO
  → HR MiniMax H3 Continuation Analyzer
  → Qwen3.5 分析最后 22 帧
  → H3 prompt
  → 现有 MiniMax H3 Conditioning（负责参考图、positive 和目标 latent）
  → HR MiniMax H3 Continuation Apply（只追加 video/audio keyframe）
  → SelfLift Progressive Sampler (MiniMax H3)
```

数据边界：

- MiniMax conditioning 负责文本 token、参考图片 latent、`minimax_refs` 和目标 H3 AV latent；
- Analyzer 负责尾帧视觉分析和 H3 续拍提示词；
- Apply 保留上游 `cross_attn`、`minimax_token_tags`、`minimax_refs`，只追加最后 22 帧 video keyframe 和可选 audio keyframe；
- SelfLift 不连接 `external_continuation`，只接 Apply 输出的 `positive` 和 `latent`。

## 二、初始问题

初始 SelfLift 设置：

```text
总步数：约 25/26
transition_step：16
lowres_scale：0.5
rho：0
highres_tiling：true
upscaler：learned H3 latent upscaler
```

表现：

- 前约 0.92 秒清晰；
- 约第 2 秒开始明显变模糊；
- 参考图实际已进入 conditioning，并非参考图缺失；
- 前 22 帧为强约束区，之后才主要体现 SelfLift 的真实生成质量。

初始视频分段 `blurdetect`（越低越清晰）：

| 时间段 | Blur mean |
|---|---:|
| 0–0.92 秒 | 6.94 |
| 0.92–2 秒 | 9.02 |
| 2–5 秒 | 9.50 |

## 三、transition_step 对照

### 3.1 从 16 降到 10

保持其他参数不变。

结果：

| 时间段 | step=16 | step=10 |
|---|---:|---:|
| 0–0.92 秒 | 6.94 | 6.91 |
| 0.92–2 秒 | 9.02 | 7.50 |
| 2–5 秒 | 9.50 | 7.92 |

结论：

- 后续生成部分模糊度下降约 17%；
- 人物结构保持正常；
- 说明原先留给完整分辨率阶段的步数不足。

### 3.2 从 10 降到 8

结果：

- 画质没有进一步改善；
- 出现肢体漂移和变形；
- 说明低分辨率阶段过短，整体姿态和运动结构尚未稳定就进入高分辨率阶段。

结论：

```text
transition_step=10 是当前约 25/26 步配置下的结构稳定下限。
```

不应继续降低到 8。

## 四、latent阶段诊断

启用 `latent_diagnostics`，记录：

- `low_resolution_endpoint`
- `lifted_endpoint`
- `final_output`
- `pixel_anchor`（仅在启用时）

采样时间位置：

```text
0.5 秒、1.5 秒、3.5 秒
```

Learned upscaler诊断示例：

| 时间 | 低分辨率终点归一化梯度 | Upscaler后 | 最终输出 |
|---|---:|---:|---:|
| 0.5 秒 | 0.358 | 0.310 | 0.382 |
| 1.5 秒 | 0.414 | 0.323 | 0.376 |
| 3.5 秒 | 0.376 | 0.302 | 0.349 |

判断：

- 低分辨率终点仍保留较多相对空间细节；
- learned latent upscaler阶段首次下降约 13%–22%；
- 后续完整分辨率采样恢复了一部分细节，但未完全恢复。

因此轻微模糊的主要首次来源是lift阶段，不是参考图丢失。

## 五、Nearest lift 同seed对照

对照设置：

```text
seed：fixed
transition_step：10
lowres_scale：0.5
upscaler_model：none（nearest）
highres_tiling：true
rho：0
```

画质测量：

| 时间段 | Learned | Nearest |
|---|---:|---:|
| 0–0.92 秒 | 6.41 | 6.37 |
| 0.92–2 秒 | 6.81 | 6.59 |
| 2–5 秒 | 7.03 | 6.74 |

Nearest略清晰，但出现：

- 左侧人物裂开；
- 随后马上重新合拢；
- 瞬时结构错误明显。

帧间运动指标：

```text
Learned：平均帧差 4.46，最大 7.92
Nearest：平均帧差 5.36，最大 9.32
```

结论：

- Nearest保留更多高频数值；
- 但不能正确重建人物结构；
- 后续H3步骤虽然会把结构拉回，但会出现可见的裂开/合拢；
- Nearest不适合作为最终方案。

## 六、最终稳定结果

恢复：

```text
transition_step：10
lowres_scale：0.5
upscaler：learned H3 latent upscaler
highres_tiling：true
rho：0
```

用户反馈：

- 画质“好很多”；
- 本次生成肢体没有出现漂移；
- 结构稳定性明显优于 `transition_step=8` 和 nearest lift。

稳定基线结论：

```text
transition_step = 10
lowres_scale = 0.5
learned H3 latent upscaler
highres_tiling = true
rho = 0
```

暂不：

- 把 `transition_step` 降到 8；
- 改用 nearest lift；
- 把裁掉前 22 帧当作模糊修复。

## 七、音频观察

曾有一次主观声音异常。客观检测：

- 无静音；
- 无削波；
- 视频约 5.1667 秒；
- 音频约 5.1520 秒；
- 32 kHz双声道；
- 22帧边界后响度约下降3 dB。

示例：

| 时间段 | RMS |
|---|---:|
| 0–0.92 秒 | -13.94 dB |
| 0.92–2 秒 | -17.09 dB |
| 2–5 秒 | -16.10 dB |

需要后续固定seed继续确认问题属于：

- 接缝跳变；
- 原声音重复；
- 人声变调；
- 音画不同步；
- 或单纯随机生成差异。

## 八、诊断产物

启用SelfLift节点的：

```text
latent_diagnostics = true
```

报告保存到：

```text
K:\ComfyUI\output\selflift_diagnostics\selflift_*.json
```

报告只记录小型标量，不额外VAE解码，不长期保留中间tensor。

## 九、后续方向

如果继续改善剩余轻微模糊：

1. 保持稳定基线和固定seed；
2. 不再修改 `transition_step`；
3. 只评估learned upscaler输出恢复策略；
4. 如测试pixel-anchor混合，必须从保守范围开始，并检查是否重新引入肢体漂移；
5. 每次只改变一个变量；
6. 同时比较结构稳定性、blurdetect、latent归一化空间梯度和声音接缝。
